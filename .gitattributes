# Normalise line endings so the repo is consistent across Windows/Linux/macOS.
* text=auto eol=lf

# Generated artefacts that Power BI reads as-is: keep LF.
*.tmdl text eol=lf
*.json text eol=lf
*.py   text eol=lf
